[← 인덱스로](../AgentCollaboration_Plan.md)
# 04. 반박 검증과 완전성 비평

종류: 리서치 · 작성: 2026-09-18 claude(다중 에이전트 워크플로 결과를 정리) · 웹 사실은 URL과 확인 날짜, 저장소 사실은 경로 기준

반박 3렌즈 × 2회차(각 회차 뒤 보수). 아래는 마지막 회차의 지적이며 최종안(02) summary의 '수정 2회차' 절에 반영 내역이 있다.

## 배선 검증: per_agent_wiring 의 기전·변경·검증 항목을 공식 문서(2026-09-18 재열람)와 저장소·사용자 홈 파일에 대조
- 반박됨: True

| 심각도 | 주장 | 왜 틀렸나 | 수정 |
|---|---|---|---|
| major | 공통(Serena) 항목: "~/.serena/projects/에 TDGame 폴더가 없어(OKGame만) AGENTS.md 131행의 project.yml 경로는 현재 미검증" → Tools/README 0b절에 'Serena 3조건은 `serena project health-check` 실측 후 project.yml 실제 경로 기입'(migration 3). | 폴더와 파일이 실제로 존재하며 AGENTS.md 131·133행의 내용(언어 서버 cpp, LLVM clangd 경로)이 이미 그 파일에 들어 있다. 즉 131행 경로는 '미검증'이 아니라 지금 검증 가능하고, migration 3의 '실측 후 기입' 전제가 거짓이다. 또 `serena` 실행 파일은 PATH에 없고 `uvx`만 있어 설계안이 적은 명령 그대로는 실행되지 않는다. | per_agent_wiring 공통 항목의 '폴더 없음·미검증' 문장을 삭제하고 'project.yml 경로·cpp·clangd 설정 확인됨(2026-09-18)'으로 교체. migration 3의 0b절 이동은 131·133행 내용을 그대로 옮기되 명령을 `uvx --from git+https://github.com/oraios/serena serena project health-check`로 적는다. OP-30의 Serena 메모리 위치도 '저장소 .serena/memories'가 아니라 '~/.serena/projects/TDGame/.serena/memories/(현재 0건)'로 정정. |
| major | OP-31·Antigravity 항목: Antigravity MCP 설정은 사용자 홈 `~/.gemini/config/mcp_config.json`(사용자 관리) 한 곳이며 완료 보고에 변경 요청만 적는다(7번째 파일, D-19). | Antigravity 공식 문서는 IDE·CLI에 대해 작업 공간 안 `.agents/mcp_config.json`(저장소에 커밋 가능)을 두 번째 설정 위치로 명시한다. 따라서 unreal-mcp·blender 항목을 저장소 파일로 두면 OP-31 동기화 대상(현재 5벌)에 포함시켜 다른 클라이언트와 같은 방식으로 관리할 수 있는데, 설계안은 이 경로를 아예 다루지 않아 D-19가 '사용자 홈 파일을 어떻게 고칠지' 결정으로 좁게 잡혀 있다. | OP-31 클라이언트 목록을 5벌 → 6벌(`.agents/mcp_config.json` 추가, 서버 ini 포함 7벌)로 고치고 D-19를 '저장소 .agents/mcp_config.json 신설(Run-BlenderMCP.ps1 방식) + 홈 파일은 삭제·유지 여부'로 재정의. migration 1 배선 실측에 'Antigravity IDE·데스크톱이 .agents/mcp_config.json 의 unreal-mcp 를 인식하는지'를 추가하고, 데스크톱이 무시하면 그때만 홈 파일 안내를 남긴다. deferred 8 sync_mcp_config.py 대상에도 포함. |
| minor | Codex 항목 change_needed: "선택: `.codex/config.toml`에 `[features] codex_git_commit = true`". | 이 기기의 Codex 0.154.0에서 해당 기능 플래그는 'removed' 상태라 설정해도 아무 효과가 없다. 공식 문서 검색에도 등장하지 않는다. | 해당 문장을 삭제한다. 커밋 형식(OP-32)은 AGENTS.md 8절 규칙과 Worklog 로만 유지. |
| minor | Codex 항목 mechanism: "스킬은 저장소 .agents/skills → ~/.agents/skills → ~/.codex/skills" 및 verified_by "`/skills`에 td-* 2개(같은 name 중복 노출 없음)". | 현행 공식 문서의 스킬 탐색 위치는 REPO `.agents/skills`(cwd→저장소 루트) → USER `$HOME/.agents/skills` → ADMIN `/etc/codex/skills` → SYSTEM 이며 `~/.codex/skills`는 문서에 없다(레거시 탐색 여부 미문서화; 로컬에 `~/.codex/skills/.system` 이 존재해 여전히 읽을 가능성은 있음). 또 문서는 같은 name 스킬을 병합하지 않고 둘 다 노출한다고 명시하므로, `~/.codex/skills/td-combat-animation-quality` 삭제(D-20, 사용자 결정 게이트)가 끝나기 전에는 migration 1의 '중복 노출 없음' 검증이 실패할 수 있다. | mechanism 문장을 문서 순서대로 고치고 `~/.codex/skills`는 '문서 미기재·레거시 가능성, 실측'으로 표기. verified_by 는 'td-* 노출 수를 기록'으로 바꾸고 '중복 없음'은 D-20 적용 후 재검증 항목으로 옮긴다. |
| minor | Gemini CLI 항목: 확인 명령은 `/memory list`·`/memory show`·`/memory refresh\|reload`. | 공식 명령 참조에는 `/memory` 하위 명령으로 list·show·refresh 만 있고 `reload`는 없다(`/skills`에는 reload\|refresh 가 있어 혼동한 것으로 보임). | `/memory refresh`로만 적는다. |
| minor | Gemini CLI 항목: "`context.fileName`(문자열 또는 배열)… 같은 폴더 다중 이름 동시 로드는 문서 미기재"(그래서 배열 실패 시 문자열 대체 계획). | 공식 GEMINI.md 문서가 `"context": {"fileName": ["AGENTS.md", "CONTEXT.md", "GEMINI.md"]}` 를 예시로 직접 제시하므로 'AGENTS.md 배열 지정'은 문서화된 사용법이다. 미기재라고 적으면 실측 우선순위와 D-13 문구가 과도하게 보수적이 된다(단, 같은 폴더의 두 파일이 모두 병합되는지는 여전히 명시적 문장이 없음). | mechanism 문장을 '배열 지정과 AGENTS.md 예시는 공식 문서에 있음; 같은 폴더 두 파일 병합 여부만 `/memory list`로 실측'으로 정정하고, 문자열 대체 시 전역 GEMINI.md 미로드 부작용 서술은 그대로 둔다. |
| minor | Cursor 항목 verified_by: "스킬 목록의 td-* 중복 수(2 기대)를 Worklog에 기록"; 공통 항목 "같은 스킬이 Cursor에서 2회 노출되나 기능 영향 없음". | Cursor 문서는 .agents/skills·.cursor/skills·.claude/skills·.codex/skills 등 여러 위치를 읽는다고만 하고, 같은 name 중복을 어떻게 처리하는지(중복 표시·병합·우선순위)는 기재하지 않는다. '2 기대'는 근거 없는 기대값이어서 실측 결과가 1이어도 '실패'로 오판될 수 있다. | 기대값을 빼고 '관측된 노출 수와 어느 경로가 표시되는지'만 기록하도록 바꾼다. |
| minor | Antigravity 항목 및 deferred 10: "Antigravity 훅 지원 여부는 미확인(확인 후 추가)". | Antigravity 변경 기록에 훅 구성 로드 시 검증 항목이 있어 훅 기능 자체는 2.6.0(2026-08-07)부터 존재한다. '미확인'이 아니라 '설정 파일 경로·이벤트 이름만 확인 필요'로 좁혀야 deferred 4·10 설계에 반영된다. | deferred 4·10 항목에 'Antigravity 훅 존재 확인(2.6.0+); 설정 파일 위치·이벤트명은 문서 확인 후 기입'으로 수정. |

확인된 항목:
- Claude Code: CLAUDE.md만 읽고 AGENTS.md는 읽지 않음, Windows에서는 심볼릭 링크 대신 `@AGENTS.md` 임포트 권장, 임포트 파일은 시작 시 로드, 재귀 4홉, 블록 HTML 주석 제거(코드 블록 안은 보존), 4MiB 초과 파일 건너뜀·200줄 권장, `/context`의 Memory files 로 로드 확인 — https://code.claude.com/docs/en/memory (2026-09-18)
- Claude Code 스킬: 탐색 경로는 `.claude/skills`·`~/.claude/skills`·플러그인·중첩 `.claude/skills`뿐이며 raw 문서(https://code.claude.com/docs/en/skills.md) 에 `.agents` 문자열 0건; description+when_to_use 1,536자 절단; `/skills` 존재 (2026-09-18)
- Codex AGENTS.md: `~/.codex/AGENTS.override.md`→`~/.codex/AGENTS.md`→git 루트부터 cwd까지 이어 붙이고 뒤(가까운) 파일이 우선, `project_doc_max_bytes` 기본 32KiB는 합계 기준, `project_doc_fallback_filenames` 존재, 검증 명령 `codex --ask-for-approval never "Summarize the current instructions."` — https://learn.chatgpt.com/docs/agent-configuration/agents-md (2026-09-18)
- Codex 설정: 프로젝트 `.codex/config.toml`은 신뢰된 프로젝트에서만 로드되며 C:\Users\jjh\.codex\config.toml 에 `[projects.'c:\project\tdgame'] trust_level = "trusted"` 존재; `codex features list`(0.154.0)에서 `hooks stable true`, `memories stable false`(문서 "off by default") — 설계안의 '훅 기본 활성·메모리 기본 꺼짐' 일치
- Codex 전역 파일: C:\Users\jjh\.codex\AGENTS.md 27줄·2,154B, 27행 "AGENTS.md 참고 하지 않는다"; codex-cli 0.154.0 로컬 확인
- Antigravity 버전: 레지스트리 DisplayVersion 데스크톱 2.14.0, IDE(User) 2.0.2(product.json ideVersion 2.0.2); `agy` 없음(PATH·%LOCALAPPDATA%\agy·~/.gemini/antigravity-cli 모두 부재)
- Antigravity 규칙·컨텍스트: 작업 공간 루트 GEMINI.md/AGENTS.md 자동 파싱은 https://antigravity.google/docs/cli/gcli-migration/ (및 ?app=antigravity 변형)에 기재, 전역 `~/.gemini/GEMINI.md`; 규칙 파일 12,000자 상한은 https://antigravity.google/docs/rules-workflows/ 의 `.agents/rules` 기준이며 루트 AGENTS.md 적용 여부는 미기재(설계안 서술과 일치); 변경 기록 2.11.0(2026-08-26) `@path` 인라인 in AGENTS.md, 2.14.0(2026-09-15) 최신; 스킬 `.agents/skills`(https://antigravity.google/docs/skills/)
- Antigravity 홈 MCP 파일: C:\Users\jjh\.gemini\config\mcp_config.json 존재, blender 항목이 `uvx blender-mcp`라 D-19 전제(런처 방식 불일치) 사실
- Gemini CLI: 로컬 0.35.3; 2026-05-19 발표·2026-06-18 무료·Pro/Ultra 요청 중단(https://developers.googleblog.com/an-important-update-transitioning-gemini-cli-to-antigravity-cli/); C:\Users\jjh\.gemini\settings.json `security.auth.selectedType: "gateway"`; gemini-cli #12345 'Closed as not planned'; `.agents/skills` 별칭·스킬 활성화 확인 프롬프트·`/skills list` (https://geminicli.com/docs/cli/skills/)
- Cursor: 루트 AGENTS.md 프런트매터 없이 자동 로드·중첩 지원(https://cursor.com/docs/rules); 스킬 경로 .agents/skills·.cursor/skills·~/.agents·~/.cursor + 호환 .claude/skills·.codex/skills·~/.claude/skills·~/.codex/skills → D-20 '3중 노출' 전제 성립; VS Code `chat.useAgentsMdFile`·중첩은 실험(https://code.visualstudio.com/docs/copilot/customization/custom-instructions)
- Agent Skills 표준(https://agentskills.io/specification): name ≤64자 소문자·하이픈·폴더명 일치, description ≤1,024자, 본문 500줄 이하 권장 — OP-22 상한과 일치; 정본 description 실측 318자(AnimationAuthoring)·173자(BlenderAnimation)
- AGENTS.md 실측: 15,859B, CRLF, 개행 165개(마지막 줄 개행 포함), 코드 스팬 밖 bare `@` 0건, `<!--` 0건; 절 제목 1~14 위치(5,13,21,38,47,54,65,76,85,108,117,150,156,162행); 131~134행 = Serena clangd 조건·UBT 절대 경로 명령·clangd 19.1.2 주의·대체 절차, 140행 = ini 포트 8000 구성, 141~143행 = 클라이언트 5벌·에디터 기동 명령·라이브 코딩 — 설계안의 이동 범위 서술과 일치
- MCP 배선: Config/DefaultEditorPerProjectUserSettings.ini 5~6행 `ServerUrlPath=/mcp`·`ServerPortNumber=8000`; .mcp.json·.codex/config.toml·.cursor/mcp.json·.gemini/settings.json·.vscode/mcp.json 5벌 모두 존재하고 unreal-mcp가 http://127.0.0.1:8000/mcp, blender가 Run-BlenderMCP.ps1 절대 경로(OP-19 위반은 기존 상태)
- GEMINI.md 실측: 37행, `@` 0건(4행 AGENTS.md는 백틱) — '`@./AGENTS.md` 제거'는 실제로는 '두지 않음'; 이동 대상 줄 15~20(세션 시작), 22~25(작업 흐름), 27~31(도구 없을 때), 34(TDGen_ 재생성), 36(`ue_editor.py start`) 존재; 이동처 Tools/WorldGen/README.md '## 한 번에 실행'(5행), Tools/README.md '## 0. 시작 전 점검'(7행)·'## 4. 도구가 없을 때 만드는 규칙'(66행)·'## 5. 실측된 함정'(73행) 존재, Tools/README.md 79줄
- 사용자 홈 파일 줄 번호: ~/.gemini/GEMINI.md 25행 '불분명한 부분이 있다면, 작업을 중단하세요', 30행 'AGENTS.md 참고 하지 않는다'; ~/.claude/CLAUDE.md 16행 '프로젝트의 AGENTS.md을 참고 하세요', 20행 '메모리에 저장'; ~/.claude.json projects 키 'C:\\Project\\TDGame'·'C:/Project/TDGame' 2개(D-24 전제 사실)
- 저장소 상태: `.agents/`·`.claude/` 없음(스텁 신설 전제 맞음), `~/.agents` 없음; .gitignore 89행 `[Ss]aved/`·93행 `.serena/`; Tools/WorldGen/capture_views_mcp.py 14행 `sys.path.insert(0, r"C:\Users\jjh\.claude\projects\C--Project-TDGame\tools")`; `rg "\bOP-\d{2}\b"` 저장소 0건(규칙 ID 충돌 없음)

## 저장소 적합성 검증 — 최종안의 경로·형식이 기존 AGENTS.md 14절, Tools/README.md, Docs/Tasks/README.md, .gitignore, 세 대장·Docs/Validation 관례와 충돌·중복하는지 실제 파일(2026-09-18 시점 작업 트리)을 열어 대조
- 반박됨: True

| 심각도 | 주장 | 왜 틀렸나 | 수정 |
|---|---|---|---|
| major | OP-29·file_layout: 세 대장 맨 위에 `\| 상태 \| 개수 \| ID 목록 \|` 3열 표를 두고, migration 6에서 'tasks_recount.py 대상 glob을 인자로 확장(5줄)'만 하면 된다 | Docs/Tasks/phase-*.md 5개는 이미 2열 `\| 상태 \| 개수 \|` 표를 갖고 있고 Tools/tasks_recount.py가 정확히 그 2열 헤더를 정규식으로 찾아 다시 쓴다. 표를 3열로 바꾸면 정규식이 매치되지 않아 스크립트가 오류 없이 조용히 아무것도 갱신하지 않게 된다(기존 결정론 도구가 무력화). 또 '인자로 확장'은 이미 구현되어 있어(sys.argv[1:]) 계획된 5줄 변경은 존재하지 않는 문제를 고치는 것이다. 07·AnimationAuthoring_Tasks에는 상태표 자체가 없고 AnimationAuthoring_Tasks는 `- 상태: **done (2026-09-16)**` 형태라 같은 정규식(`[A-Za-z]+`)에 걸리지 않는다. | migration 6의 tasks_recount.py 변경 내용을 '출력 형식을 3열(ID 목록 포함)로 바꾸고 `**` 굵게 표기·괄호 날짜를 허용하며 07·AnimationAuthoring_Tasks에 표가 없으면 삽입'으로 정정한다. 또는 기존 2열 표는 그대로 두고 그 아래에 별도 `\| 상태 \| ID 목록 \|` 표를 추가해 스크립트 정규식과 충돌하지 않게 한다. 어느 쪽이든 deferred 1(status_snapshot.py) 이전까지 P 대장의 표 유지 주체가 스크립트임을 OP-29 집행 열에 적는다. |
| major | OP-13: '실행 도구는 Tools/<영역>/에만 두고(공용 라이브러리는 Tools/ 루트)' + OP-14 승인 동사 15개 규칙, 그리고 deferred_automation 스크립트 8개를 Tools/ 루트에 둔다 | 기존 관례는 Tools/ 루트 = '범용 (실행) 도구'다(README 1절, 루트에 run_in_editor.py·uemcp.py·ue_editor.py·editor_inspect_level.py·tasks_recount.py·check_orphan_external_actors.py·find_unused_content.py·editor_regenerate_pcg.py 8개 실행 스크립트). OP-13의 문구대로면 이 8개와 설계안 자신의 deferred 스크립트 8개(status_snapshot·tools_registry·agent_check·lessons_lookup·find_tool·sync_agent_skills·validation_index·sync_mcp_config) 전부가 위치 위반이다. 이름도 OP-14 승인 동사(generate build make validate check inspect capture export import author render prepare place setup select)에 `sync`·`find`가 없고 명사_명사 형태라 8개 중 7개가 위반이다. 규칙 채택 직후 설계안 자신의 첫 산출물이 규칙을 어기게 되어 risks에서 우려한 '규칙 권위 훼손'을 설계안이 스스로 만든다. | OP-13을 '실행 도구는 Tools/<영역>/ 또는 Tools/ 루트(1절 범용 도구·공용 라이브러리, 등록 필수)에만 둔다'로 고친다. OP-14 승인 동사에 `find`·`sync`(또는 `check`로 통일)를 넣거나 deferred 스크립트 이름을 승인 동사형으로 바꾼다(예: make_status_snapshot.py, check_tools_registry.py, check_agent_ops.py, find_lesson.py, find_tool.py, make_skill_stubs.py, make_validation_index.py, check_mcp_config.py). 기존 루트 8개 중 이름 위반(find_unused_content·editor_regenerate_pcg·tasks_recount)은 allowlist 대상으로 명시한다. |
| major | AGENTS.md 11절 이동 시 '127~130·135~136·140·144~148행은 그대로' 둔다 | 그대로 두는 135행은 'Serena 메모리에는 프로젝트 구조와 반복 절차만 기록하고'라고 하여 프로젝트 사실 기록을 허용하는데, 새 OP-30과 Tools/README 0b절 1줄은 'Serena 메모리에 프로젝트 사실을 두지 않는다(선호·절차 교정·L-ID 포인터만)'라고 한다. 항상 로드되는 규칙 파일 안의 기존 문장과 새 규칙이 같은 저장소(Serena 메모리)에 대해 정반대를 말하게 된다. | migration 2에서 135행을 'Serena 메모리에는 사용자 선호·절차 교정과 L-ID 포인터만 둔다(OP-30); 비밀 정보·개인 정보는 기록하지 않는다'로 교체하고, 이동·유지 행 목록에서 135행을 '수정'으로 옮긴다(바이트 예산 영향 ±50B). |
| minor | OP-24: 증거는 `Docs/Validation/<영역>/<YYYY-MM-DD>_<작업ID>_<설명>.<md\|png\|json\|gif>`, md 머리는 `작업: / 교훈: / 명령:` 3줄 | 기존 Docs/Validation 관례는 (1) ID 앞·하이픈·날짜 뒤(`P3-07-pcg-biome.md`, `P1-pie-checks-2026-09-12.md`, `worldgen-builder-seed7-2026-09-12.md`), (2) 대장이 지정한 평면 경로(`Docs/Validation/P1-01-world-settings.png`, `P1-09-streaming-profile.md`), (3) 하위 폴더 `ashen-vale/`의 밑줄 `.jpg`(`cpp_dungeon_slot0.jpg`), (4) md 머리는 `# <ID> <제목> (<날짜>, <agent>)` 제목줄 + `스크립트:`·`실행 순서:` 줄이다. 새 명명은 날짜 앞·밑줄·영역 폴더·3줄 머리로 네 가지를 동시에 바꿔 세 번째 규약을 만들고, `.jpg`를 확장자 목록에서 빠뜨려 기존 캡처 형식을 배제한다. | 명명을 기존 관례에 맞춰 `Docs/Validation/<영역>/<작업ID 또는 L-ID>-<설명>-<YYYY-MM-DD>.<md\|png\|jpg\|gif\|json>`(하이픈, ID 앞)로 바꾸고, md 머리는 기존 제목줄 형식 `# <ID> <제목> (<날짜>, <agent>)`을 유지한 채 `교훈:`·`명령:` 2줄만 추가한다. 영역 폴더 표에 기존 `ashen-vale`(지역명)·`BlenderAnimation`을 별칭으로 넣는 계획은 유지. |
| minor | 스킬 스텁·deferred 6은 '줄 끝 LF 고정', tasks_recount.py는 LF로 쓰며 정본↔스텁 동일성을 검사한다 | 이 저장소는 `core.autocrlf=true`이고 .gitattributes(27줄)에 eol/text 규칙이 없어 체크아웃 시 텍스트 파일이 CRLF로 바뀐다(AGENTS.md 165줄 전부 CRLF). LF로 써도 다음 체크아웃·브랜치 전환 뒤 CRLF가 되어 '고정'이 성립하지 않고, 바이트 단위 동일성 검사와 AGENTS.md 바이트 예산(줄당 1B 차이, 약 165~195B)이 줄 끝에 따라 흔들린다. 최종안은 eol 규칙을 D-24로 보냈으면서 규칙 문장에는 'LF 고정'을 그대로 남겼다. | 'LF 고정' 문구를 삭제하고 'D-24에서 `.gitattributes`에 `*.md text eol=lf` 등 규칙을 정하기 전까지는 줄 끝을 규정하지 않는다'로 바꾼다. deferred 6·3의 동일성·바이트 검사는 줄 끝 정규화 후 비교하도록 Automation_Backlog 항목에 명시한다. |
| minor | Docs/Tasks/README.md 규칙 1조를 'AGENTS.md 15절 순서를 따른다'로 축약하고 마지막 줄 '마지막 갱신 2026-09-09'를 삭제한다 | 1조(33행)에는 축약 대상 외에 '아키텍처 문서(03)의 클래스·폴더 이름을 따른다'는 영역 고유 규칙이 함께 있고, 43행에는 '마지막 갱신' 외에 'Phase별 완료 현황은 각 파일 맨 위 표를 갱신한다'가 함께 있다. 지시대로 축약·삭제하면 두 문장이 유실된다. | 1조는 '시작 순서는 AGENTS.md 15절. 아키텍처 문서(03)의 클래스·폴더 이름을 따른다'로, 43행은 '마지막 갱신' 구절만 지우고 표 갱신 문장은 남긴다. |
| minor | AGENTS.md 141~143행을 Tools/README.md 0b절로 옮기고 각 자리에 포인터 1줄만 남긴다 | 141행에는 규칙 문장('주소나 포트를 바꿀 때는 다섯 파일과 에디터 설정을 함께 바꾼다' = OP-31)이, 143행에는 어느 OP에도 없는 사용자 보호 규칙('에디터를 닫을 때는 저장되지 않은 에셋이 있는지 사용자에게 먼저 알린다')이 들어 있다. OP-01은 Tools/README에 규칙을 두지 않고 OP-ID로 인용하라고 하므로 원문을 그대로 옮기면 OP-01 위반이고, 143행 문장은 항상 로드되는 파일에서 사라진다. | 0b절에는 141행의 설정 파일 목록만 두고 규칙 문장은 'OP-31'로 인용한다. 143행의 알림 문장은 AGENTS.md 11절 포인터 줄에 1구절로 남기거나(예산 +60자) OP-31 또는 15절 종료 절차에 '에디터 종료 전 미저장 에셋 확인(ue_editor.py stop이 중단)'로 흡수한다. |
| minor | OP-13: Content/Python에는 register()/unregister()를 가진 상주 모듈만 둔다 | Content/Python/init_unreal.py는 언리얼이 에디터 시작 시 자동 실행하는 부트스트랩(3줄: import + register() 호출)이라 자체 register()/unregister()가 없다. 문구대로면 상주 툴셋을 켜는 유일한 진입점이 규칙 위반이 된다. | OP-13에 '`init_unreal.py`(엔진 부트스트랩, register 호출만)는 예외' 1구절을 추가한다. |
| minor | Tools/README 2b절·D-22에서 'smoke_test.py'를 공용 클라이언트 역할 도구로 지칭한다 | 같은 이름의 파일이 두 곳(Tools/AnimationAuthoring/smoke_test.py, Tools/BlenderMCP/smoke_test.py)에 있고 Tools/ 루트에는 없다. 파일명만으로는 등록표 행·OP-12 검색 결과·D-22 결정 대상이 특정되지 않는다. | 최종안·등록표·D-22 문안에서 `Tools/AnimationAuthoring/smoke_test.py`(TDMcpAnimationClient 보유)와 `Tools/BlenderMCP/smoke_test.py`를 경로로 구분해 적는다. |
| minor | migration 3에서 Tools/README 1~3b절 표 열을 `도구 \| 환경 \| 용도 \| 예 \| 상태` 5열로 손편집해 통일한다 | 현재 표는 1절 3열(도구\|용도\|예), 2·3절 2열(도구\|용도), 3b절 2열(호출\|용도)로 약 40행이며, 3b절 행은 C++ 함수·커맨드릿·콘솔 명령이라 '환경(system/editor/pie/blender/mcp/ps)' 어휘에 맞지 않는다. 같은 구간을 deferred 2(tools_registry.py --write)가 마커 사이에서 다시 생성하므로 손편집은 곧 덮어써질 이중 작업이다. | 지금은 각 절 표에 `상태` 열 하나만 추가(또는 절 아래 '상태 메모' 1줄)하고, 5열 통일은 tools_registry.py --write 도입 시 스크립트가 한 번에 수행하도록 Automation_Backlog에 적는다. 3b절은 '환경' 대신 '호출 경로(python/cmdlet/console)' 열로 둔다. |
| minor | 최종안에 인용된 수치·줄 번호: AGENTS.md 166줄, GEMINI.md 37줄, 07 §6 '762행', 추적 .pyc 15개, `[← 인덱스로]` 관례 12개 문서, GEMINI.md '15~21·26~31행(세션 시작·접근 경로·도구 없을 때)', 07 §6 5항의 'Saved/CombatSim·measurements 규정' | 실측과 다르다: AGENTS.md는 최종안 자신의 지표(`(Get-Content).Count`)로 165줄, GEMINI.md 36줄, Docs/Validation 언급 줄은 764행(5항), 추적 .pyc 14개, 역링크 관례 문서 34개, GEMINI.md '접근 경로'는 10~13행(15~21은 세션 시작 절차, 27~31이 도구 없을 때), 07 §6 5항은 `measurements/`만 규정하고 `Saved/CombatSim/<날짜>/`는 M1 항목 산출물 칸(351행)에 있다. OP-02가 '원문 줄 번호로 이동을 증명'하라고 요구하므로 틀린 번호가 완료 보고에 그대로 옮겨진다. | 해당 수치를 165줄·36줄·764행·14개·34개·10~13행으로 정정하고, OP-24의 MonsterAI 예외 문장은 '07 §6 5항의 `Docs/MonsterAI_CombatSim/measurements/`(M1 결과 폴더는 MD-05 전까지 `Saved/CombatSim/<날짜>/`, 07:351)'로 나눈다. |

확인된 항목:
- 규칙 ID 접두어 `OP-`, 교훈 ID `L-`, 개명 ID `AD-`는 저장소(Docs·Tools·AGENTS.md·GEMINI.md, Saved 제외)에서 0건 — 충돌 없음(`rg -n '\bOP-\d{2}\b'`, `\bL-[A-Za-z]*-?\d+\b`, `\bAD-\d+` 모두 0건)
- Docs/Tasks/decisions.md 마지막 항목은 D-11(66행) — 새 '관리 체계' 절 D-12~D-24 번호 여유 있음; D-06 실측 문장은 44행에 존재
- Docs/AnimationAuthoring_Tasks.md D절(154~171행)의 D-01~D-09는 표 안 ID이며 애니메이션 문서·Tools/AnimationAuthoring·BlenderAnimation 어디에도 외부 참조 0건 — `AD-` 개명 안전. E-01(184행)·E-02(188행) 내용이 최종안의 done/superseded 처리 근거와 일치
- 기록 칸 형식 `YYYY-MM-DD 동사(<agent>): 내용`은 기존 관례 `2026-09-12 완료(Claude): ...`(phase-2-dungeon-vertical-slice.md:19 등)와 일치. 기존 항목은 ` / `로 이어 붙이므로 그 구분자를 유지하면 됨
- 작업 항목 필드는 현재 정확히 9개(Docs/Tasks/README.md:18-29) — '담당 추가로 10필드' 계산 정확. 세 대장 모두 `### <ID>` 제목(P: `### P1-01`, M: `### M0-01`(07:99), A: `### A-01`(41행))이라 4단계 `rg -n -A 8 "^### <ID>"` 부분 읽기가 성립
- AGENTS.md 이동 대상 131~134행(Serena clangd·UBT 절대 경로 명령)·141~143행(클라이언트 5벌·에디터 기동·라이브 코딩) 내용 일치, 실측 847자/1,225B·674자/1,206B로 최종안 수치와 ±3 이내. 유지 대상 127~130·140·144~148행 존재. 현재 15,859B는 정확
- Tools/WorldGen/capture_views_mcp.py:14가 `sys.path.insert(0, r"C:\Users\jjh\.claude\projects\C--Project-TDGame\tools")`, :15가 `import uemcp` — 최종안의 유일한 코드 수정 대상과 일치하며 Tools/uemcp.py 존재
- Tools/ue_editor.py:21-26에 ROOT·ENGINE·EDITOR_EXE·BUILD_BAT·MCP_PORT 상수가 이미 있음 — OP-19 '절대 경로는 ue_editor.py 상단 상수 한 곳' 관례가 기존과 일치(LLVM·clangd 상수만 추가)
- 도구 머리 docstring 관례가 이미 존재(Tools/templates/editor_tool_template.py:1-6, tasks_recount.py:1-4, ue_editor.py:1-13) — OP-15 4줄 docstring과 AGENTS.md 5절 예외 1줄은 기존 관행의 명문화이며 5절 49행과 충돌하지 않음
- 기존 Tools/README 4절 이름 규칙 `Tools/<영역>/editor_<동사>_<대상>.py`(68행)·`<동사>_<대상>.py`(70행), 4행 `editor_*.py`는 run_in_editor.py 실행 — OP-14 접두어 확장(pie_|blender_|mcp_)과 호환
- Tools/README.md 5절은 정확히 6건(74~79행), 현재 79줄, 0절은 167자 — 포인터 1줄 대체·≤600자 예산 성립. 절 번호 '2b'는 기존 '3b'(49행) 관례와 같은 형식. 마커 `tools_registry`·`status:start`는 저장소에 0건
- `.gitignore`: 89행 `[Ss]aved/`가 Saved/Locks·Saved/AgentOps를 이미 덮고 93행 `.serena/` 존재; `__pycache__/`·`*.pyc`·`Tools/scratch/*`·`!Tools/scratch/.gitkeep` 규칙은 없어 4줄 추가가 중복되지 않음. 추적 중 .pyc는 14개(D-18 `git rm --cached` 대상)
- `.gitattributes`에 `*.png`·`*.gif`·`*.jpg` LFS 규칙이 이미 있음(19~27행) — OP-24 'png·gif는 LFS'는 .gitattributes 변경 없이 성립(AGENTS.md 8절 79행 요구 충족)
- 하위 폴더 AGENTS.md·CLAUDE.md·GEMINI.md 0개(루트 2개뿐), `.claude/`·`.agents/` 폴더 부재 — OP-01·스텁 4개·루트 CLAUDE.md 신설에 충돌 없음
- 신설 예정 경로 전부 부재 확인: CLAUDE.md, Docs/NOW.md, Docs/Worklog, Docs/Lessons, Docs/README.md, Docs/AgentRules.md, Docs/Animation_Plan.md, Docs/Automation_Backlog.md, Tools/scratch, Tools/_archive. `Docs/<주제>_Plan.md` 명명은 기존 인덱스 2개와 동일 관례
- GEMINI.md 22~25행(작업 흐름 Generate→Validate→Select→Bake, 캡처 직접 판단·Docs/Validation), 34행(TDGen_* 전체 삭제 후 재생성), 36행(`ue_editor.py start`) 내용 일치. 이동처 Tools/WorldGen/README.md '## 한 번에 실행'(5행)과 '## PCG 그래프 스크립팅 실측'(49행) 절 존재
- MCP 클라이언트 설정 5벌 존재(.mcp.json, .codex/config.toml, .cursor/mcp.json, .gemini/settings.json, .vscode/mcp.json). .gemini/settings.json에 `context` 키 없음, .codex/config.toml에 `[features]` 없음 — 추가가 기존 키와 충돌하지 않음
- 스킬 정본 frontmatter: Tools/AnimationAuthoring/SKILL.md name=td-animation-authoring(description 318자), Tools/BlenderAnimation/SKILL.md name=td-combat-animation-quality(173자) — 최종안 수치·이름 일치. Docs/AnimationAuthoring.md:26이 `$CODEX_HOME/skills` 수동 복사 문단 맞음
- Docs/Validation/README.md는 GAS 전환 전 기록 설명(1~13행) — 재작성 대상 판단 맞음. MonsterAI 04 §3.4(221행), 07 §6(756행), MD-05(691행) 존재; 07 §6 5항이 'Docs/Validation은 MD-05 결정 전까지 쓰지 않는다'라 최종안의 게이트 처리와 정합
- 대장 집계 일치: doing 15(phase-2: 6, phase-3: 9), blocked 7(07), decision 3(07 1·phase-0 1·phase-2 1) — migration 5 계획 수치 정확
- AGENTS.md 12절(153행)·13절(160행)의 상태 표기 `todo/doing/blocked/done/decision`과 'doing으로 시작·done으로 종료·decisions.md에 결정' 절차는 OP-11·OP-28·OP-29와 충돌 없음; 15절 신설은 기존 14절 번호를 바꾸지 않음

## 부담 검증: 세션 시작 읽기 예산 실측 재계산, 세션 종료 절차의 사람·에이전트 부담, 규칙 수와 규칙 간 모순, 6개월 뒤 썩을 항목
- 반박됨: True

| 심각도 | 주장 | 왜 틀렸나 | 수정 |
|---|---|---|---|
| blocker | AGENTS.md 편집 후 '약 9,377자·≈17,160B·약 193줄'로 OP-02 상한(소프트 ≤195줄, 하드 ≤200줄) 안에 들어온다 | 설계안이 스스로 정한 15절 구성만 세어도 시작 5+종료 5+필수 규칙 9+식별자 1+영역 표 7(5행+머리 2)+포인터 1+작성 규칙 2+두지 말 것 3 = 33행 + 절 제목 1 = 34행이다. 현재 165행(Get-Content 실측; 설계안의 166은 오차) − 이동 7행 + 포인터 2행 + 1·5·8·9·10·12·13절 추가 11행 + 34행 = 205행으로 하드 200행을 넘는다. 설계안의 두 완화(144~148행 추가 이동 +포인터 1, 시작·종료 4줄화)를 모두 써도 199행이라 소프트 195행을 넘고 빈 줄 0개·핵심 함정 0줄이어야 겨우 하드 안이며, OP-26이 약속한 '핵심 함정 ≤6줄 승격' 여유는 없다. 문자 예산도 마찬가지로, 설계안 문구를 최소로 옮긴 15절 초안이 42행·2,313자·3,442B로 '≤1,400자'를 65% 초과했고(문자 수 참고치 합계 ≈10,190자, 바이트 ≈18,357B로 소프트 18,432B에 75B 차이), 15절 안에서 시작·종료 10줄과 OP-04·OP-05 요약이 같은 내용을 두 번 적어 약 700자를 낭비한다. | ① 15절에서 시작 5줄·종료 5줄을 삭제하고 [A] 9개 요약만 남긴다(OP-04·OP-05가 곧 시작·종료 절차). ② 줄 수 상한을 버리고 바이트 단일 지표(소프트 18KiB·하드 20KiB)만 둔다. 줄 수를 꼭 두려면 하드 220줄. ③ migration 2 착수 전에 15절 초안 실물을 만들어 `(Get-Item).Length`·`(Get-Content).Count`를 재고, 초과분은 11절 144~148행 이동으로 흡수한다. ④ 15절 문자 예산을 ≤2,000자로 고쳐 적는다. |
| major | 세션 시작 4단계에서 `rg -n -A 8 "^### <ID>"`로 대장 항목 본문을 읽으면 된다 | 실제 대장 항목은 9~17행이고 재개에 가장 중요한 `- 기록:` 칸이 마지막 행이다. -A 8은 15개 doing 항목 중 13행 이상인 9개(P2-04·P2-05·P2-09·P3-00·P3-11·P3-07 등)에서 완료 조건 뒷부분과 기록 칸을 잘라 버린다. 매 세션 조용히 정보가 사라지는 절차 결함이다. | `rg -n -A 18 "^### <ID>"` 또는 '다음 `^### ` 직전까지' 범위 읽기로 바꾼다(항목 최대 1,502자이므로 2개 읽어도 ≤3,000자, 예산 영향 미미). 4단계 예산을 '항목 1~2개 ≤3,000자'로 정정한다. |
| major | 명령은 PowerShell 우선이며 `rg -c "^### L-" Docs/Lessons/*.md`, `rg -n "<키워드>" Tools/README.md Tools/*/README.md Tools/*/SKILL.md`, `rg -n "^### L-" Docs/Lessons/*.md` 같은 검사·색인 명령이 그대로 동작한다 | PowerShell은 네이티브 실행 파일에 넘기는 인수의 와일드카드를 확장하지 않아 rg가 리터럴 `*.md`를 받고 'os error 123'으로 종료한다. 설계안이 정한 주 셸에서 migration 4 검증 명령, Lessons 색인 명령, 도구 생성 절차 1단계 명령이 모두 실패한다. | 규칙·절차의 모든 rg 명령을 '디렉터리 인수 + `--glob`' 형태로 통일한다(예: `rg -c "^### L-" --glob '*.md' Docs/Lessons`, `rg -n "<키워드>" --glob 'README.md' --glob 'SKILL.md' Tools`). AgentRules 3절에 'PowerShell에서는 경로 와일드카드 대신 --glob' 1줄을 둔다. |
| major | OP-14 승인 동사 15개(generate build make validate check inspect capture export import author render prepare place setup select)로 새 스크립트 이름을 짓는다 | 설계안 자신이 예약한 deferred_automation 스크립트 9개(status_snapshot.py, tools_registry.py, agent_check.py, lessons_lookup.py, find_tool.py, sync_agent_skills.py, validation_index.py, sync_mcp_config.py, resource_lock.py)가 하나도 이 규칙에 맞지 않는다(동사가 없거나 find·sync처럼 목록 밖이거나 동사가 두 번째 토큰). 규칙 제정자가 첫 구현에서 위반하는 규칙은 설계안이 '위험' 절에서 스스로 경고한 '규칙 권위 하락'을 즉시 일으킨다. 기존 tasks_recount.py·ue_editor.py·uemcp.py·run_in_editor.py도 같은 위반이다. | 둘 중 하나: (a) 예약 스크립트명을 규칙에 맞춰 `check_agent.py`·`make_status_snapshot.py`·`check_tools_registry.py`·`inspect_lessons.py`·`select_tool.py`·`export_skill_stubs.py`·`build_validation_index.py`·`export_mcp_config.py`로 바꾼다, 또는 (b) 동사 목록을 '권장'으로 내리고 금지 동의어 4개(create/bake/verify/test)만 규칙으로 남긴다. 어느 쪽이든 Automation_Backlog의 이름과 OP-14를 같은 변경에서 맞춘다. |
| major | 세션 종료 6단계에서 '에디터·Blender 상태는 원복(PIE 종료, TDGen_ 라벨 임시 액터만 삭제)'한다 | `TDGen_` 라벨은 임시 표식이 아니라 베이크된 정식 생성물(태양·대기·안개·포스트프로세스·ISM 인스턴스·영속 테스트 상자·PCG 볼륨)에 붙는 라벨이고, 도구용 액터만 `TDGen_Tool_*`다. '임시'를 구분할 규약이 없으므로 규칙을 문자대로 따르면 레벨의 베이크 결과를 지운다. AGENTS.md 146행(삭제는 명시적 승인 후)과 GEMINI.md 34행(TDGen_*는 재생성 스크립트가 지우는 생성물)과 모순되는 삭제 지시다. | 종료 6단계를 'PIE 종료, 저장하지 않은 임시 변경은 보고만, 액터 삭제 없음'으로 바꾼다. 굳이 정리하려면 `TDGen_Tool_*` 라벨만 명시하고 그것도 사용자 승인 뒤로 둔다. |
| major | 대장 3곳 맨 위에 `\| 상태 \| 개수 \| ID 목록 \|` 표를 두고 4단계가 이 표만 읽으며, tasks_recount.py는 대상 glob만 확장(5줄)하면 된다 | phase-*.md 맨 위 표는 이미 tasks_recount.py가 정규식 `\\| 상태 \\| 개수 \\|\n\\|---\\|---\\|`로 재생성하는 2열 표다. 3열로 바꾸면 정규식이 맞지 않아 스크립트가 조용히 아무것도 안 바꾸고(new == text), ID 목록 열은 손으로 유지되어 '개수는 스크립트, ID는 손'이라는 두 관리자가 한 표에 생긴다. 상태를 바꿀 때마다 항목·표·NOW ①절 세 곳을 고쳐야 하므로 몇 세션 안에 표가 낡고, 그 표만 읽는 4단계는 낡은 ID로 청구하게 된다. | 2열 표는 그대로 두고, 4단계는 `rg -n "^- 상태: (doing\|blocked\|decision)" -B 4 --glob 'phase-*.md' Docs/Tasks`(현재 9,695자, migration 5 정리 후 ≈3,000자)로 ID를 얻는다. ID 목록 열이 꼭 필요하면 migration 6에서 tasks_recount.py가 3열 표(개수+ID)를 생성하도록 10줄을 고치고 07·AnimationAuthoring_Tasks도 같은 스크립트가 대상에 넣는다(손 유지 표 0개). |
| major | 규칙 정본은 두 층이고 [A] 9개만 두 파일에 같은 문구로 두어 어긋남을 deferred 3의 difflib로 잡는다 | 세션 시작·종료 절차가 저장소 안에 네 번 적힌다: AGENTS.md 15절 시작 5줄·종료 5줄, 같은 절의 OP-04·OP-05 한 줄 요약, Docs/AgentRules.md 2절 OP-04·OP-05 전문, 같은 파일 3절의 7단계·8단계 전문(여기에 9절 작업 전·중·후 3줄까지 더하면 다섯 번). difflib 대조는 [A] 요약 대 전문 한 쌍만 보므로 나머지 세 사본은 검사 밖이고, 6개월 뒤 어느 것이 정본인지 알 수 없게 된다. 항상 로드되는 파일에 같은 절차를 두 번 싣는 비용(≈700자/세션)은 설계안이 강조한 토큰 절약과도 어긋난다. | AGENTS.md 15절에는 [A] 9줄만 두고 시작·종료 10줄을 뺀다. OP-04·OP-05 전문은 'AgentRules 3절 N단계를 따른다' 포인터로 축소해 단계 목록은 3절 한 곳에만 둔다. deferred 3 difflib 대상은 '15절 [A] 9줄 ↔ AgentRules 2절 [A] 9줄'로 명시한다. |
| major | OP-04·OP-05·OP-06의 '지금' 집행은 다음 세션 에이전트가 완료 보고의 `읽음:`·`기록 갱신:` 두 줄을 실제 파일과 대조하는 것이다 | 완료 보고는 그 에이전트의 채팅 출력이라 저장소에 남지 않고, 다음 세션(다른 벤더일 수 있음)은 그것을 볼 수 없다. 실제로 대조 가능한 것은 파일뿐이며, 그 검사는 이미 OP-09(NOW 헤더 < Worklog 마지막)와 OP-07(헤더 3일 초과)이다. 따라서 Worklog·NOW를 둘 다 안 쓴 세션은 3일이 지나야 드러나고, `읽음:` 줄은 사용자에게 검토 의무가 없으므로 읽는 사람이 없다. 집행 수단으로 기술된 것이 실재하지 않는다. | `읽음: …` 한 줄을 Worklog 항목 안에 넣어 파일로 남기고(그러면 다음 세션이 실제로 대조 가능), OP-04~06의 '지금 집행' 서술을 'OP-09·OP-07의 파일 대조가 유일한 검출이며 아무것도 안 쓴 세션은 3일 규칙으로만 잡힌다'로 정직하게 고친다. 보고 두 줄은 '사용자 열람용'으로 등급을 내린다. |
| minor | Docs/NOW.md는 40줄·2,000자 이하이며 ②절에 D-12~D-24와 MD-05를 등재한다 | 결정 13건을 `경로#ID` 한 줄씩 적으면 그것만 13행·약 900자다. 오늘 상태(doing 15·blocked 7·decision 3)를 그대로 넣으면 첫 NOW가 약 41행·2,500자로 상한을 넘고, migration 5로 doing을 3건까지 줄여도 29행·1,757자로 여유가 12%뿐이라 첫 작업부터 상한과 싸운다. 사용자가 결정을 미루면 이 13행은 몇 달 동안 그대로 남는다. | ②절에 '관리 체계 미결 13건 → Docs/Tasks/decisions.md#관리-체계' 한 줄만 두고 개별 ID는 결정 파일에서 읽게 한다. 상한은 유지. |
| minor | 2단계는 `Docs/Worklog/<이달>.md` 마지막 5항목을 읽고 OP-09는 그 마지막 항목 날짜와 NOW 헤더를 비교한다 | 매월 1일 첫 세션에는 이달 파일이 없거나 항목이 0~4개라 2단계가 읽을 것이 없고 OP-09의 비교 대상이 사라진다. 1년에 12번 반복되는 절차 구멍이다. | 2단계에 '이달 파일이 없거나 5항목 미만이면 전월 파일 꼬리를 이어서 5항목' 1구절을 넣는다. |
| minor | 스킬 스텁 4개는 손편집하지 않고 동기화는 deferred 6이 한다 | deferred 6은 미구현이므로 정본 description을 200자로 축약하거나 고칠 때 스텁을 갱신할 합법적 수단이 없다. 규칙이 자기 실행을 막는다. | OP-22에 'deferred 6 구현 전까지는 정본 frontmatter를 그대로 복사하는 손 갱신 허용' 1구절을 넣는다. |
| minor | 4단계에서 착수 항목의 '참조' 칸이 지목한 절만 부분 읽기한다(세 대장 공통) | 애니메이션 대장에는 `- 참조:` 필드가 한 건도 없어 이 절차가 적용되지 않는데, migration 6의 애니메이션 대장 손질 목록(상태 어휘·담당·AD- 개명·상태표·포인터·E-01/E-02)에 참조 필드 추가가 없다. | migration 6에 '애니메이션 항목에 참조 칸 추가(문서·절 지정)'를 넣거나 4단계에 '참조 칸이 없으면 Docs/Animation_Plan.md 문서 순서표의 해당 문서 절만'을 명시한다. |
| minor | Tools/README.md 0b절에 Serena project.yml 실제 경로를 실측 후 기입하고, OP-19로 새 문서에 사용자 홈 절대 경로를 쓰지 않는다 | project.yml은 사용자 홈(`C:\Users\jjh\.serena\...`) 아래에 있으므로 실제 경로를 적으면 OP-19를 같은 변경에서 위반한다. | 0b절에는 `~/.serena/projects/<프로젝트>/.serena/project.yml`처럼 홈 상대 표기만 쓰고 OP-19에 '`~/` 표기는 허용' 1구절을 둔다. |
| minor | Worklog 항목은 ≤5줄·≤400자이고 마지막 5항목 합계 ≤2,000자다 | 사양대로(변경 파일·검증 명령과 결과·증거 경로·적중·주의) 정상적인 항목을 쓰면 헤더 포함 6행·440자라 400자를 넘긴다. 상한이 첫 항목부터 어겨지면 몇 주 안에 사문화되고 5항목 합계는 2,200~3,000자가 된다. | 항목 상한을 ≤500자로, 5항목 합계를 ≤2,500자로 정정하고 계수 표에는 실측치를 적는다(예산 영향 +500자). |
| minor | 2주 운영 후 'Worklog 항목 수/세션 수'와 'NOW 헤더 갱신율'을 세어 스크립트 착수 순서를 정한다 | 세션 수를 기록하는 곳이 없고(Worklog 항목은 세션당 1개 이상이라 세션과 1:1이 아님), NOW.md 이력은 커밋에만 남는데 커밋은 사용자 지시 때만(최근 6일 5회) 하므로 세션 단위 갱신율을 복원할 수 없다. 6개 지표 중 2개가 측정 불가다. | 세션 수는 Worklog `시작` 항목 수로 정의하고, NOW 갱신율은 'NOW 헤더 날짜·agent가 Worklog 마지막 항목과 일치하는 횟수/전체 항목'으로 Worklog만에서 계산되게 바꾼다. |
| minor | deferred 2 tools_registry.py는 Tools/**/*.py를 대상으로 하고 scratch·_archive·templates·__pycache__만 제외한다 | Tools/BlenderMCP 아래에 git 무시 폴더 .venv(파이썬 1,149개)·.runtime(26개)이 있어 파이썬 glob으로 걷으면 1,175개를 검사한다. rg는 .gitignore를 존중해 문제가 없지만(OP-12 표본 검색 20파일) 파이썬 스크립트는 그렇지 않다. | deferred 2 사양에 'git ls-files 기준(추적 파일만)' 또는 '.venv·.runtime 제외'를 명시한다. |
| minor | 07-roadmap 762행이 증거 위치 규정('Docs/Validation/은 MD-05 결정 전까지 쓰지 않는다')이고 AGENTS.md는 현재 166줄이다 | 증거 위치를 정한 §6 항목은 5항이며 실제 줄은 764행이고, 762행은 3항(완료 조건·검증 근거)이다. AGENTS.md는 165줄이다. 줄 번호 인용은 편집 한 번에 낡으므로 규칙·절차 문서에 줄 번호를 쓰면 6개월 뒤 대부분 틀린다. | 저장소에 들어가는 문서(AgentRules·Backlog·decisions '관리 체계')에서는 줄 번호 대신 절·항 번호(예: '07 §6 5항')로 인용하고, 줄 번호는 완료 보고에만 쓴다. |
| minor | 손 유지 이중화를 줄였으므로 6개월 뒤 썩는 항목이 크게 줄었다 | 여전히 손으로만 유지되고 재검증 트리거가 없는 항목이 남는다: ① Docs/AgentRules.md 4절 계수 표(드라이런 1회 뒤 갱신 주체 없음), ② Tools/README.md 0b절의 도구 버전·경로(clangd 22.1.2·Antigravity 2.14.0·MCP 설정 7벌 목록), ③ Lessons L-repo-01~04 배선 사실(Antigravity 2.11.0 `@path`, Gemini CLI 0.35.3 context.fileName 등 벤더 버전 종속), ④ GEMINI.md 3행의 `/memory list`·`/memory show` 명령명, ⑤ Docs/README.md '언제 읽는지' 열과 Animation_Plan 도구 세대 표, ⑥ 스킬 스텁 4벌의 description 사본, ⑦ [C] 보류 규칙(OP-11[C]·deferred 11)처럼 활성 조건만 있고 소유자가 없는 문장, ⑧ OP-14 동사 목록. 이들은 설계안의 '2주 검토'에 점검 항목으로 들어 있지 않다. | migration 11 검토 항목에 '계수 표 재측정, 0b절 버전·경로 실재 확인(`Get-Command clangd`·설정 파일 존재), L-repo 항목 범위 버전 vs 현재 버전 대조, 스텁 4벌 diff'를 넣고, 각 손 유지 표에는 `확인: YYYY-MM-DD <agent>` 한 줄을 두어 낡음을 눈으로 보이게 한다. [C] 규칙은 AgentRules 5절 '보류' 절로 옮겨 본문 규칙 수를 31개로 줄인다. |

확인된 항목:
- 매 세션 고정 읽기 예산은 실측으로 재계산해도 크게 어긋나지 않는다: AGENTS.md 편집 후 ≈10,200자(설계안 9,400자, +9%) + 진입 파일 ≤400자 + NOW ≤2,000자(표본 1,757자) + Worklog 5항목 ≈2,200자(표본 440자×5) + 스킬 메타 ≈450자 ≈ 15,200자(설계안 14,300자, +6%); 영역 진입 포함 ≈23,000~24,000자로 설계 목표 '≤23,000자'와 거의 같다(4단계 -A 8 잘림을 고치면 +1,500자).
- 토큰 환산은 보수적이다: tiktoken 실측 AGENTS.md 0.48(o200k)/0.68(cl100k) 토큰/자, Tools/README.md 0.41/0.49, 07 대장 0.49/0.63로 설계안 가정 0.6~0.9보다 낮아 토큰 절감 주장(54~85%)의 방향은 유지된다.
- 이동 대상 줄의 실측치가 설계안과 일치한다: AGENTS.md 131~134행 844자/1,222B, 141~143행 672자/1,204B(설계안 1,226B/1,207B, 오차 ≤4B); 바이트/문자 비율 1.846(설계안 1.83); `Select-String -Pattern '(^|\s)@[A-Za-z./]' AGENTS.md` 현재 0건으로 검사식이 동작한다.
- 저장소 사실 인용 대부분이 정확하다: GEMINI.md 15~21·22~25·26~31·34·36행 내용, ~/.codex/AGENTS.md 27행·~/.gemini/GEMINI.md 30행 'AGENTS.md 참고 하지 않는다', AnimationAuthoring_Tasks.md 27행·252~257행 실측 메모, .gitignore 89행 `[Ss]aved/`·93행 `.serena/`, Docs/Tasks/README.md 9필드(담당 추가 시 10필드), capture_views_mcp.py 14행 Claude 홈 sys.path(`parents[1]`=Tools 교체안 정확, Tools/uemcp.py 존재), 정본 SKILL description 318자·173자, 저장소 내 `OP-\d{2}` 0건.
- OP-12 검색 비용은 감당 가능하다: rg가 .gitignore를 존중해 Tools/BlenderMCP/.venv(1,149 py)·.runtime을 건너뛰며 표본 검색(`capture|screenshot`)이 20파일만 반환했다.
- 대장 항목 머리 형식 `### <ID>` + `- 상태:`가 P(phase-*.md)·M(07) 대장에서 일관되어 ID 기반 rg 부분 읽기 자체는 성립한다(A 대장은 설계안 말대로 정규화가 필요: `**done (2026-09-16)**`·`**부분` 형태 존재).
- 세션 종료 쓰기 부담은 에이전트에게 과하지 않다: NOW 표본 1,757자 + Worklog 440자 + 대장 항목 기록 1~2줄 + 보고 2줄 ≈ 2,300자·5~7회 도구 호출로 설계안 추정(1,000~2,500자)과 맞는다. 사람 부담은 결정 13건 1회와 드라이런 3~4세션 관찰뿐이며 세션마다 검토 의무가 없다는 점도 적절하다.
- 규칙 32개 자체는 과다하지 않다: 매 세션 로드되는 것은 [A] 9개뿐이고 [B] 21개는 행위 직전 부분 읽기(≈300자)라 세션당 비용이 작다. 문제는 개수가 아니라 절차의 4중 서술(별도 항목으로 지적)이다.
- Codex 32KiB·Antigravity 12,000자 한도는 편집 후에도 충족된다: 프로젝트 AGENTS.md ≈18.4KB + 전역 2,154B < 32KiB; 문자 ≈10,200자 < 12,000자.

## 완전성 비평
### 빠진 것
- **파일 인코딩 규칙과 PowerShell 쓰기·읽기 함정이 어디에도 없다. 실측: Docs 아래 .md 86개 전부 UTF-8·BOM 없음(BOM=0/86), Tools/tasks_recount.py:22도 utf-8·BOM 없음으로 쓴다. 그런데 Windows PowerShell 5.1에서 `Add-Content`/`Set-Content`(추가 전용 Worklog에 가장 자연스러운 명령)는 ANSI(CP949)로 쓰고, `>`·`Out-File -Encoding utf8`은 BOM을 붙이며, `Get-Content`는 BOM 없는 UTF-8 한글을 기본 인코딩으로 잘못 읽어 줄이 합쳐진다(이번 검증에서 Docs/MonsterAI_CombatSim/07-roadmap-and-tasks.md의 `## 6.` 위치가 Get-Content 기준 729행, rg 기준 756행으로 어긋남). 설계안은 줄 끝(D-24)만 다루고 인코딩은 다루지 않는다.**
  - 왜: Codex·Gemini·Antigravity가 셸로 Docs/Worklog·Docs/NOW.md·Docs/Lessons를 쓰면 첫 세션부터 CP949·BOM 파일이 섞여 rg 검색(OP-27, 6단계)과 `읽음:` 대조(OP-09)가 조용히 실패한다. 또 OP-02의 측정 명령 `(Get-Content AGENTS.md).Count`·`Select-String ... AGENTS.md`가 기본 인코딩으로 줄을 합쳐 잘못된 줄 수·0건 오탐을 낼 수 있다.
  - 채우는 법: OP-02 또는 새 OP 한 줄: '저장소 텍스트 파일은 UTF-8·BOM 없음. 장부 파일 쓰기는 Python(`open(..., encoding="utf-8", newline="\n")`) 또는 `[IO.File]::WriteAllText(경로, 본문, [Text.UTF8Encoding]::new($false))`만 쓰고 `Add-Content`/`Set-Content`/`>`를 쓰지 않는다'. OP-02 측정 명령을 `Get-Content -Encoding UTF8` 또는 `rg -c '' AGENTS.md`·`python -c`로 교체. 이 함정을 migration 1에서 L-repo-05로 기록하고 15절 핵심 함정 초기 승격 후보에 올린다.
- **하위 에이전트(서브에이전트)의 장부 쓰기 권한·식별자 규칙이 없다. 사용자 전역 ~/.claude/CLAUDE.md 2·3행이 '하위 에이전트를 실행합니다'를 강제하고, 설계안 CLAUDE.md 3행은 하위 에이전트에 15절·NOW 요약을 '전달'만 한다.**
  - 왜: 하위 에이전트가 OP-11대로 항목을 청구하거나 Worklog·NOW를 쓰면 '에이전트당 동시 doing 1개'가 깨지고, 같은 분에 같은 agent 항목이 여러 개 생겨 항목ID(`HH:MM claude`) 1:1 대조(OP-06)가 무너진다. Codex·Antigravity도 서브에이전트를 지원하므로 Claude만의 문제가 아니다.
  - 채우는 법: OP-10 또는 OP-11에 1줄: '하위 에이전트는 NOW·Worklog·대장·Lessons를 쓰지 않고 결과를 부모에게 돌려주며, 부모가 세션당 1회 기록한다. 식별자는 부모와 동일'. CLAUDE.md 3행을 '전달 + 쓰기 금지'로 고친다.
- **중단된 세션(컨텍스트 소진·크래시·사용자 종료)을 검출하는 규칙이 3일 규칙(OP-07)뿐이다. 7단계가 `시작` 항목을 먼저 쓰게 하므로 '마지막 항목이 `시작`인데 같은 agent의 `완료|막힘` 항목이 없다'는 상태가 파일에 남는데 OP-09는 NOW 헤더 날짜 vs Worklog 마지막 날짜 한 조건만 본다.**
  - 왜: 중단 세션은 대장에 doing·담당을 남긴 채 사라지며, 사용자가 같은 날 다른 에이전트를 돌리면 그 항목이 청구 불가로 막힌다. 3일을 기다릴 필요 없이 다음 세션이 0비용으로 알 수 있는 신호를 버리고 있다.
  - 채우는 법: OP-09에 두 번째 조건 추가: 'Worklog 마지막 5항목 중 `시작` 뒤에 같은 agent·같은 작업ID의 `완료|막힘|결정`이 없으면 중단 세션으로 보고 NOW ②절에 `중단 의심: <항목ID>` 1줄, 담당 필드는 사용자 확인 뒤 비운다'. deferred 3 --start 경고 목록에도 추가.
- **15절 핵심 함정을 '초기 0줄'로 시작하면서 승격 조건을 'Worklog `적중:` 2회'로만 두었다. Worklog는 채택 시점에 비어 있으므로 이미 여러 곳에 중복 기록된 함정(Git Bash `/Game/` 경로 변환: Docs/AnimationAuthoring_Tasks.md:29 + Claude 메모리 tdgame-mcp-preflight; 새 C++ 모듈 라이브 코딩 불가: Tools/README.md:74 + Claude 메모리 tdgame-worldgen-pipeline; delete_asset 잔존·툴팁 1024자: AnimationAuthoring_Tasks.md:250~251 + 메모리)은 다시 두 번 겪어야 승격된다.**
  - 왜: 사용자 요구 (a)'필요한 도구를 알고 작업'의 핵심이 함정 회피인데, 도입 직후 몇 주 동안은 AGENTS.md에 함정이 0줄이라 rg 검색(6단계)을 건너뛴 세션이 같은 실패를 반복한다. 기존 중복 기록은 사실상 '서로 다른 세션·에이전트 2회 적중'의 증거다.
  - 채우는 법: OP-26에 부트스트랩 조항 1줄: 'migration 4 이관 시 서로 다른 원위치 2곳 이상에 있던 항목은 적중 2회로 간주해 승격 후보 목록을 사용자에게 제시하고 승인분만 15절에 넣는다(≤6줄·바이트 재측정)'. migration 4 검증에 '중복 출처 수' 열 추가.
- **OP 규칙 자체의 추가·변경·삭제 권한이 없다. OP-28은 결정(D-)에 대해 '에이전트는 (미정) 제안만'이라 했지만 Docs/AgentRules.md의 OP-xx를 에이전트가 직접 고쳐도 되는지는 어느 규칙도 말하지 않는다. migration 11 '규칙 3~5개 조정'도 주체가 없다.**
  - 왜: 세 에이전트가 각자 편의대로 OP를 완화·추가하면 규칙 정본이 첫 달에 갈라지고, deferred 3의 '15절 ↔ AgentRules 동일 문구' 검사가 잦은 불일치를 낸다.
  - 채우는 법: OP-01에 1줄: 'OP 규칙의 추가·문구 변경·삭제는 사용자 승인 뒤에만 하며 에이전트는 Docs/Automation_Backlog.md 또는 decisions.md '관리 체계'에 (미정) 제안만 남긴다. 변경 시 Worklog `결정` 항목과 AGENTS.md 15절 동기화를 같은 변경에서'.
- **AGENTS.md 144~148행을 '소프트 초과 시 0b절로 이동'할 묶음으로 취급했지만, 그중 145행(용도는 7절 로직 정책과 같음)·146행(에디터 상태 변경 도구는 사전 고지, 삭제·덮어쓰기는 명시 승인 후)·147행(MCP 생성 에셋 LFS·git status 확인)은 절차가 아니라 안전 규칙이다. 절차인 것은 144행(도구 검색 켜짐)·148행(라이브 코딩)뿐이다.**
  - 왜: 146행은 사용자 결정 없는 삭제·덮어쓰기를 막는 유일한 공용 규칙 문장이다(Docs/Tasks/README.md:36과 07 §6 4항에도 있으나 대장 작업 세션만 읽음). 조건부 읽기 파일(Tools/README.md 0b절)로 옮기면 도구 사용 세션에서 승인 규칙이 사라진다.
  - 채우는 법: OP-02·migration 2 이동 후보를 '11절 131~134·141~143·144·148행'으로 한정하고 145~147행은 유지 또는 15절 [A]로 흡수(OP-16·OP-18과 병합)한다고 명시. 예산이 모자라면 대신 127~130행(Serena 탐색·편집 절차)을 0b절로.
- **사용자 요구 (a)의 '필요한 코드를 알고'에 해당하는 영역→코드 폴더 대응이 15절 표에 없다. 표 열은 영역|대장|결정|증거 폴더|Lessons 코드뿐이고, 14절 164행은 Source/TDGame 폴더를 나열만 할 뿐 어느 영역이 어느 폴더(예: 몬스터 AI=AI/{CombatToken,NPC}·Combat/·Source/TDGameEditor/?; 월드젠=World/Generation·Source/TDGameEditor/{Landscape,WorldGen}·Tools/WorldGen)를 쓰는지 잇지 않는다.**
  - 왜: 코드 작업 세션이 폴더를 찾으려면 대장 항목 산출물 칸이나 아키텍처 문서(02 §2, 03)를 열어야 해 4단계 '참조 칸이 지목한 절만' 예산을 넘긴다. 특히 애니메이션·전투 기반 영역은 AGENTS.md에 코드 위치 언급이 0건이다.
  - 채우는 법: 15절 영역 표에 '코드·도구 폴더' 열 1개 추가(행당 ≈40~60B, 합계 ≈300B; 예산 여유 ≈900B 안). 또는 14절 164행 끝에 '영역별 대응은 15절 표' 1구절과 표의 Lessons 코드 옆에 폴더를 병기.
- **에이전트가 만드는 에셋·원본 파일(.uasset·.blend·.fbx·.json)의 버전 복제·정본·보관 규칙이 없다. git status에 AS_/AM_TD_Player_Attack01_Heavy_RToL_v04.uasset(미추적)과 AnimationSources/..._v05.{blend,fbx,json}이 있고 AnimationSources는 26MB다. OP-17·OP-14는 스크립트 복제만 다루고, OP-18은 '애니메이션 원본은 AnimationSources/'라는 위치만 정한다.**
  - 왜: 사용자 요구 (b)'새 도구·리소스 관리'에서 리소스가 빠졌다. 버전 접미어(v04·v05) 파일이 LFS(.gitattributes: *.fbx·*.blend·*.png·*.gif·*.jpg)로 쌓이고 어느 것이 현행인지 대장·문서 어디에도 없어 다음 세션이 옛 버전을 기준으로 작업할 수 있다.
  - 채우는 법: OP-18에 1줄: '에셋·원본 버전 복제는 `<이름>_v<nn>`로 만들되 현행 버전은 해당 대장 항목 산출물 칸에 1개만 적고, 옛 버전 삭제·이동은 사용자 승인 뒤(Content는 8절 LFS 규칙)'. Docs/Animation_Plan.md에 '현행 에셋 표' 1개(스크립트 전까지 손 유지, `확인:` 줄).
- **Tools/BlenderAnimation의 미커밋 스크립트 5개(author_sword_grip.py·inspect_grip.py·prepare_grip_revision.py·render_grip_review.py·validate_grip_revision.py)를 어떻게 처리할지 설계안이 말하지 않는다. 실측: 이 5개는 Docs·Tools 어느 .md에도 0건 언급, Tools/BlenderAnimation/SKILL.md에는 스크립트 표 자체가 없다(63줄, .py 언급은 call_tool.py 1건). 설계안의 '버전 복제 5쌍은 기록'은 이 5개가 아니라 기존 19개 안의 player/weighty 쌍을 가리키는 것으로 읽힌다.**
  - 왜: 채택 첫날부터 OP-16('등록되지 않은 도구는 존재하지 않는 것으로 간주')·OP-17(1회용은 scratch)을 위반하는 파일이 사용자 작업 트리에 있어, migration 3에서 등록할지 scratch로 옮길지 정하지 않으면 규칙 권위가 첫날에 무너진다(설계안 자신이 지적한 위험).
  - 채우는 법: migration 3 목록에 '미커밋 grip 5개: 사용자 결정(등록·커밋 / Tools/scratch / AnimationSources/<이름>/scripts)'를 넣고, SKILL.md 스크립트 표 신설 시 19+5개 전부를 상태 열과 함께 등재.
- **에이전트 식별자 6개(claude|codex|gemini|antigravity|cursor|user)에 VS Code Copilot이 없다. .vscode/mcp.json이 존재하고 감사 표 1에 VS Code Copilot이 AGENTS.md 자동 로드 클라이언트로 들어 있다.**
  - 왜: OP-10은 '같은 어휘'를 요구하므로 VS Code에서 작업하면 첫 기록부터 어휘 밖 값이 생긴다. 반대로 실제로 안 쓴다면 .vscode/mcp.json을 OP-31 동기화 6벌에서 빼 유지 비용을 줄일 수 있다.
  - 채우는 법: 사용자에게 VS Code Copilot 실사용 여부를 물어 식별자 `copilot` 추가 또는 .vscode/mcp.json을 '사용자 관리·동기화 제외'로 표기.
- **'사용자가 직접 바꾼 것'을 장부에 어떻게 반영하는지 없다. 3단계는 `git status`로 사용자 변경을 '확인·보존'만 한다. 지금도 Config/DefaultEditor.ini·Docs/AnimationQuality.md·Docs/BlenderAnimationWorkflow.md가 사용자 수정 상태(M)다. 또 루트 사용자용_할일_목록.md('AI는 읽지 않습니다')와 NOW ③ '다음 행동'의 관계도 정해지지 않았고, 이 파일의 제외는 Docs/README.md 3절(필요 시 검색 파일)에만 적혀 있어 세션 시작 때 보이지 않는다.**
  - 왜: 사용자 변경이 NOW·Worklog에 없으면 OP-09 대조가 '기록 누락 의심'을 오탐하고, 에이전트가 사용자 변경 파일을 같은 세션에 건드릴 때 OP-25 5개 파일 한도·'덮어쓰지 않는다'(AGENTS.md 18행) 판단 근거가 없다.
  - 채우는 법: OP-04 3단계에 1줄: '사용자 변경 파일이 작업 대상과 겹치면 착수 전에 묻고, 겹치지 않으면 Worklog 시작 항목에 `사용자 변경: <파일 n개>`만 적는다'. 15절 '두지 말 것'에 '사용자용_할일_목록.md는 읽지 않는다' 1구절(≈40B).

### 근거 없이 단정한 것(정정 목록)
- 'ashen-vale는 dungeon 별칭'(15절 표·OP-24)은 틀렸다. Docs/Validation/P3-11-ashen-vale.md:1은 '잿빛 골짜기(Ashen Vale) 메인 지역 베이크 검증', GEMINI.md:23은 '야외: Tools/WorldGen/generate_ashen_vale.py'다. 별칭은 worldgen이어야 한다.
- OP-24의 '기존 관례대로 `<작업ID 또는 L-ID>-<설명>-<YYYY-MM-DD>`'는 관례가 아니다. Docs/Validation 최상위 .md 10개(README 제외) 중 그 형식을 온전히 따르는 것은 P1-pie-checks-2026-09-12.md 1개뿐이고, P2-12-candidates·P3-07-pcg-biome·P3-09-vertical-slice·P3-11-ashen-vale는 날짜 없음, phase1-3-cpp-…·tools-gemini-dungeon-…·worldgen-builder-seed7-…는 ID 없음, commandlet-dungeon-sweep·cpp-world-layout-seed7는 둘 다 없음. 새 규칙으로 명시해야 한다.
- '`[← 인덱스로]` 34개 문서'는 35개다(rg `^\[←` Docs --glob '*.md', 2026-09-18: 35파일 35건; UKGame_FeatureFileMap.md 포함).
- '기존 루트 8개는 allowlist'는 과장이다. Tools 루트 .py 8개 중 editor_inspect_level.py·editor_regenerate_pcg.py·check_orphan_external_actors.py·find_unused_content.py 4개는 OP-14(접두어·권장 동사 check/find)에 이미 맞고, allowlist가 필요한 것은 uemcp.py(라이브러리, 대상 아님)·ue_editor.py·run_in_editor.py·tasks_recount.py다.
- 'Docs/AnimationAuthoring_Tasks.md 실측 메모 27행·252~257행': rg 기준 Git Bash 경로 함정은 29행, `### 실측 메모` 머리는 252행이고 다음 절 `## 권장 순서`가 259행이라 범위는 252~258행이다(설계안이 재실측을 예고했으므로 수치만 정정).
- 'Serena project.local.yml·cache/cpp·memories/ 0건': 실측(2026-09-18) ~/.serena/projects/TDGame/.serena/에 project.local.yml이 존재하고 cache/cpp에 document_symbols.pkl·raw_document_symbols.pkl 2개가 있다. 0건인 것은 memories/뿐이다. project.yml의 `language_servers: - cpp`·`ls_path: C:/Program Files/LLVM/bin/clangd.exe`는 확인됨.
- 'Docs/Validation 평면 파일 20개'·'GAS 전 png 9': 최상위 파일은 24개(md 11(README 포함)·png 11·json 2)다. png 11개 중 landscape-mcp-test-editor/viewport 2개는 GAS 전 PIE 검사가 아니라 랜드스케이프 MCP 실측이므로 D-23 처분 대상에서 구분해야 한다.
- '절대 경로 하드코딩 13+개': Tools 아래 .py에서 `C:\Project|C:\Users|Program Files` 포함 파일은 18개다(2026-09-18 실측, __pycache__ 제외). ue_editor.py:22에는 이미 `ENGINE = r"C:\Program Files\Epic Games\UE_5.8"` 상수가 있어 '상수 블록 신설 5줄'은 LLVM·clangd 경로 추가로 축소된다.
- 'Tools/README.md 3b절 미등록 7개 등록': 어떤 7개인지 목록이 없어 검증 불가. Tools/README.md 3b절(49~64행)은 C++ 함수·커맨드릿·콘솔 명령·에디터 스크립트가 섞인 표라 '호출 경로' 열만으로는 7개를 특정할 수 없다.
- LLM Wiki 판단(answer_on_llm_wiki)의 웹 근거에 URL·확인 날짜가 없다: Karpathy 'LLM Wiki' gist(2026-04-04), Verdent 2026-04-06 '유지되지 않는 위키는 능동적으로 오도', Proudfrog 2026-04-08 '1건 약 35K 토큰', 'LLM 전체 lint 100문서 약 300K 토큰/회', AAIF(Agentic AI Foundation)가 AGENTS.md를 관리, agentskills.io description 1,024자·Claude Code 목록 1,536자 절단, 대안 5종(Beads v1.0 Dolt·Mem0/Zep·Cline Memory Bank·Ruler·BMAD/Spec Kit). 공통 지침(웹 사실은 URL+확인 날짜)을 어긴다.
- 배선·위험 절의 다음 주장도 URL 없이 단정됐다: arXiv 2605.10039(세션 내 준수 감쇠), openai/codex #39223(개인 메모리가 규칙을 덮어씀), gemini-cli #12345('AGENTS.md 기본 포함 not planned' — 감사가 인용한 이슈는 #16058이며 #12345는 자리표시자처럼 보임), Claude Code '파일 크기 한도 4MiB·200줄 권장', Antigravity '규칙 파일 12,000자 캡', 'Codex 훅 기본 활성·메모리 기본 꺼짐', 'Codex 같은 name 스킬은 병합되지 않고 둘 다 노출', 'Gemini CLI 2026-06-18부터 무료·Pro/Ultra 요청이 Antigravity CLI로 전환', 'Antigravity 데스크톱 2.14.0·IDE 2.0.2'(확인 명령 미기재). 참고로 Codex 0.154.0(`codex --version`)·Gemini CLI 0.35.3(`gemini --version`)은 2026-09-18 실측으로 맞다.
- 'Antigravity 12,000자 = 20KiB÷1.846': 비율 1.846은 감사의 15,694B/8,503자인데 설계안이 채택한 디스크 실측은 15,859B·8,668자(ReadAllText, 2026-09-18)로 비율 1.83이다. 결론(여유 있음)은 바뀌지 않지만 두 수치 체계를 섞어 썼다.
- 'Claude 메모리 8개 파일은 저장소에 없는 사실'·'Claude 홈 tools 14개'·'.pyc 14개': .pyc 14개(+smoke-report.json)와 홈 tools 14개는 `git ls-files`·디렉터리 실측으로 맞다. 다만 메모리 8개의 '증거 없으면 미검증' 판정은 이관 세션에서 파일별로 다시 판단해야 하며 설계안은 근거를 제시하지 않았다.
- 'PowerShell에서 rg 경로 와일드카드 실패(os error 123)'는 2026-09-18 재현으로 확인됐고(`rg -n ... Docs/Tasks/phase-*.md` → exit 2, `--glob 'phase-*.md' Docs/Tasks`는 정상), 07:351(`Saved/CombatSim/<날짜>/`)·07 §6 5항 764행·AnimationAuthoring.md:26·~/.codex/AGENTS.md:27·~/.gemini/GEMINI.md:25·30·Docs/Tasks/README.md:43·AGENTS.md 131~135·141~148행·capture_views_mcp.py:14도 맞다 — 즉 위에 적은 것 외의 줄 번호 인용은 검증됐다.

### 사용자 결정이 필요한 것
- 동시 세션 운용 여부: 한 기계에서 Claude Code(데스크톱 다중 세션 포함)·Codex·Antigravity를 동시에 돌릴 계획이 있는가? '예'면 OP-11 보류분(잠금·자원 줄·7일 회수)과 deferred 11을 채택 시점에 활성화해야 하고, '아니오'면 '에이전트당 동시 doing 1개' 규칙을 유지한다. 설계안은 '사람 1명·순차 세션'을 전제로만 두었다.
- 하위 에이전트 정책: 전역 ~/.claude/CLAUDE.md가 하위 에이전트 실행을 강제하는데, 하위 에이전트가 장부(NOW·Worklog·대장·Lessons)를 직접 쓰는 것을 금지하고 부모가 1회 기록하는 방식으로 할지, 하위 에이전트에도 별도 식별자(예: claude-sub)를 줄지.
- 채택 커밋 전 현재 작업 트리 처리 순서: `git status --porcelain` 18건(Config/DefaultEditor.ini·Docs/AnimationQuality.md·Docs/BlenderAnimationWorkflow.md 수정, AnimationSources v05 3개·Content v04 .uasset 2개·Docs/Validation/BlenderAnimation weighty-v04 5개·Tools/BlenderAnimation grip 스크립트 5개 미추적)을 먼저 커밋할지, 별도 커밋으로 남길지, 이관 커밋과 섞을지. 미커밋 grip 스크립트 5개는 등록·커밋 / Tools/scratch / AnimationSources/<이름>/scripts 중 어디로 보낼지.
- 도입 범위: 규칙 32개·새 파일 약 14개·마이그레이션 11단계를 한 번에 채택할지, 최소 핵심([A] 9규칙 + CLAUDE.md·GEMINI.md 배선 + NOW/Worklog/Lessons + Docs/AgentRules.md 골격)만 먼저 넣고 [B] 규칙과 Tools/README·Validation·인덱스 보강은 2주 운영(migration 11) 뒤 순차 도입할지. 사용자가 토큰 절약·간결을 중시하므로 설계안이 아니라 사용자가 정해야 한다.
- OP 규칙 변경 권한: 에이전트가 Docs/AgentRules.md의 OP 문구를 직접 고칠 수 있게 할지, 결정(D-)처럼 '(미정) 제안만' 하고 사용자 승인 뒤 반영하게 할지.
- 15절 초기 핵심 함정: 기존에 2곳 이상 중복 기록된 함정(Git Bash /Game/ 경로 변환, 새 C++ 모듈 라이브 코딩 불가→restart, delete_asset 잔존, 툴팁 1024자, 캡처는 CaptureViewport만 신뢰, PowerShell rg 와일드카드, 이번에 확인한 PowerShell 인코딩)을 0줄 대신 승인분만 넣을지. 넣으면 AGENTS.md 예산 ≈600~700B 소비.
- 15절 영역 표에 '코드·도구 폴더' 열(≈300B)을 추가할지, 코드 위치는 14절·아키텍처 문서에 맡길지.
- Gemini 계열 주 실행체와 VS Code 실사용: 검증 주 경로를 Antigravity 데스크톱/IDE로 두고 Gemini CLI를 부로 두는 설계안 가정이 맞는가? VS Code Copilot을 실제로 쓰는가(쓰면 식별자 `copilot` 추가, 안 쓰면 .vscode/mcp.json을 OP-31 동기화 6벌에서 제외)?
- 파일 인코딩 정책: D-24(.gitattributes eol)와 함께 'UTF-8·BOM 없음 고정'을 규칙으로 못 박을지(현재 Docs .md 86/86이 그 상태), 그리고 장부 쓰기를 Python 경로로만 허용할지.
- 사용자 변경 반영 방식: 사용자가 직접 고친 파일·에디터 작업을 Worklog에 `user` 항목으로 남길지(누가: 사용자 본인 vs 다음 세션 에이전트가 git status 관찰로), 사용자용_할일_목록.md를 NOW ③ '다음 행동'과 연결할지 완전히 분리할지.
- 에셋 버전 복제 정책: AnimationSources/·Content/Characters/Mannequins/Anims/Blender/의 v03·v04·v05 복제본 중 현행 1개만 대장 산출물 칸에 적고 옛 버전은 삭제·보관 중 무엇으로 할지(LFS 용량 26MB+, 삭제는 사용자 승인 사항). D-23(Validation 옛 증거)과 같은 결정 묶음으로 처리할지.
- Docs/AnimationAuthoring_Tasks.md 분리 여부: 감사 보고서 서술(A~G 절 설명·권장 순서)과 대장 항목이 한 파일인데, 대장만 표준 형식으로 분리하고 감사 결과는 Lessons/anim.md·Docs/Animation_Plan.md로 옮길지, 설계안대로 한 파일에서 정규화만 할지.
- 외부참고 문서 출처: UE_Engine_* 가이드 5개·TDUAF·TDMonsterAnimationOptimization_500_Research.md의 작성 주체(에이전트 생성인지 외부 자료인지)를 사용자가 아는가? 알면 머리 표기를 '생성 문서·검증 안 됨' 또는 '외부 자료·출처 URL'로 구체화하고, 모르면 설계안의 '외부참고·설계 기준 아님'으로 둔다.
- D-15 보강: Tools/Animation(Kimodo text-to-motion) 11개를 폐기 후보로 표기하기 전에 `C:\Tools\kimodo` 실제 설치 여부와 향후 text-to-motion 재사용 의사를 사용자가 밝혀야 한다(설계안·감사 모두 미확인).

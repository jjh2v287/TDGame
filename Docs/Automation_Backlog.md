[← 인덱스로](AgentCollaboration_Plan.md)
# 자동화 대기 목록 (검사·생성 스크립트)

종류: 계획 · 작성: 2026-09-18 claude

규칙(`Docs/AgentRules.md`)이 먼저이고 스크립트는 나중이다(사용자 지시). 공통 사양: 파일 열거는 `git ls-files` 기준(.venv·.runtime 제외), 텍스트 비교는 줄 끝 정규화 후, 정규식·수치 상수는 스크립트에만 둔다. 구현되면 해당 절을 지우고 docstring 포인터 1줄만 남긴다. 착수 순서는 2주 운영 뒤 Worklog 지표로 정한다(D-27).

## 1. Tools/make_status_snapshot.py(첫 번째 착수 후보, tasks_recount.py 흡수·개명) — Docs/Tasks/phase-*.md(P), Docs/MonsterAI_CombatSim/07-roadmap-and-tasks.md §3(M), Docs/AnimationAuthoring_Tasks.md(A~G)를 읽어 각 대장 맨 위 `| 상태 | 개수 | ID 목록 |` 표를 재계산하고 NOW.md `<!-- status:start -->`~`<!-- status:end -->` 구간(①②절)에 doing 목록(ID·담당·경과일, 담당 없음 '방치 확인')과 decision 목록(`경로#ID`)을 생성. ③절 보존. 표준 라이브러리만, 종료 코드 0/1. 검사 정규식은 스크립트 상단에만.

## 2. Tools/check_tools_registry.py [--check|--write] — `git ls-files` 기준 Tools/**/*.py·*.ps1·Content/Python/*.py·.gemini/scripts/*.py(scratch·_archive·templates·lib 제외; .venv·.runtime은 비추적이라 자동 제외)의 첫 docstring 4줄 파싱, 파일명 금지 동의어·권장 동사 규칙(권장은 경고), 절대 경로 grep(ue_editor.py 상수 파일 제외), 저장소 밖 sys.path, 금지 폴더 안 .py 개수를 검사; --write는 Tools/README.md 마커 구간(5열 `도구|환경|용도|예|상태`, 3b절은 호출 경로 열)과 SKILL.md 스크립트 표를 재생성(손글 절 보존); Tools/registry_allowlist.txt(경로 + 사유)로 초기 위반 약 30개(루트 8개 포함)를 허용하며 줄어드는 개수를 보고; 미등록·유령 행·헤더 누락 1건 이상이면 exit 1. ue_editor.py ensure 끝에서 --check 호출.

## 3. Tools/check_agent_ops.py — `--start [--agent]`: NOW.md 본문 + Worklog 마지막 5항목(월초는 전월 꼬리 포함) 출력 + 경고 ≤5줄(NOW 헤더 3일 초과, NOW 헤더 < Worklog 마지막 날짜, 대장 doing과 NOW ①절 불일치, 미등록 도구 수, scratch 30일 초과, 결정 대기 수). `--end --agent <id>`: 오늘 해당 agent Worklog 항목 존재·`읽음:` 줄 존재, NOW 헤더 오늘·agent, 이번 세션 새 .py 등록 여부. `--config`: AGENTS.md 바이트(하드 20,480B; 줄 수는 경고)·코드 스팬 밖 bare `@`·`<!--`; CLAUDE.md `@AGENTS.md`·≤5줄; GEMINI.md ≤6줄·`@` 임포트 없음; 하위 폴더 규칙 파일 0개; MCP 설정 5벌 URL과 ini 포트 일치; 규칙 ID 접두어 `OP-`가 AgentRules 밖에서 정의되지 않음; '15절 [A] 9줄 ↔ AgentRules 2절 [A] 9줄' difflib(줄 끝 정규화); 전역 3파일 금지 문장 잔존 경고(읽기만). `--docs`: 상대 링크·백틱 경로 실존, 인바운드 0 문서(외부참고 제외), 채택 커밋 이후 문서의 머리 3줄, 경로 없는 `D-?\d+` 인용, superseded 대상 실존, AgentRules와 70% 이상 동일 문단, md 안 `C:\\` 절대 경로 목록. `--lessons`: 필수 4필드, 증거·범위 경로 실존, 증상 중복, superseded 사슬, 범위 'UE 5.x' vs EngineAssociation, AGENTS.md `(L-…)` 역링크 실존, Worklog `적중:` 2회 이상 미승격 후보, 미검증 개수, L-repo 범위 버전 vs 현재 버전. `--repo`: git ls-files의 __pycache__·*.pyc·Tools/**/*.json, 최근 30커밋 접두어 통계. 출력 ≤40줄 + Saved/AgentOps/check_<날짜>.md, 종료 코드 0/1/2. 모든 정규식·한도는 스크립트 상단 상수 한 곳.

## 4. Tools/find_lesson.py "<오류 텍스트>" + 실패 훅 — Lessons `증상:` 필드에 대해 4자 이상 토큰 2개 이상 일치 항목 상위 3건 `L-ID | 해결 한 줄 | 경로:줄` 출력. 별도 hits.log 없음(적중 기록은 Worklog `적중:`, 집계는 deferred 3). 훅: Claude `.claude/settings.json` PostToolUseFailure(Bash) → additionalContext; Codex `.codex/hooks.json` PostToolUse(비0 종료) → additionalContext(프로젝트 훅 신뢰 승인 1회); Gemini `.gemini/settings.json` AfterTool → hookSpecificOutput.additionalContext; Antigravity 훅(2.6.0+ 존재 확인)은 설정 파일 위치·이벤트명을 문서에서 확인한 뒤 추가. 단일 파이썬 진입점, 출력 ≤10줄.

## 5. Tools/find_tool.py <키워드…> — deferred 2 헤더 색인(Saved/AgentOps/registry.json)·파일명·Content/Python def·Source/TDGameEditor UFUNCTION·MCP 툴셋 캐시(`uemcp.py list` 24시간)를 한 번에 검색, 동의어 표 자동 확장, `일치도|종류|경로|한 줄` 표 ≤10건 + Worklog `도구` 항목 초안(`검색: …`) 출력. OP-12 검색 명령을 대체.

## 6. Tools/make_skill_stubs.py [--write|--check] — Tools/<영역>/SKILL.md frontmatter를 읽어 .agents/skills/<name>/·.claude/skills/<name>/ 스텁을 생성·갱신, name 규격·description 길이(표준 1,024자, 권장 200자 경고)·정본 본문 ≤500줄 검사, 불일치(줄 끝 정규화 후 비교) 시 exit 1, 심볼릭 링크 불사용, 사용자 홈 쓰기 없음. 구현 전까지 손 복사 허용(OP-22).

## 7. Tools/make_validation_index.py — Docs/Validation/**를 걸어 명명 규칙 적합('규칙 이전' 표시), md 제목줄 + 2줄 머리, 5MB 초과, LFS 추적 여부, 어느 md에서도 참조되지 않는 미디어(현재 33개), 대장이 지정한 경로와의 대응을 수집해 Docs/Validation/INDEX.md를 생성. 기존 파일 개명 없음. 유효 증거↔대장 ID 표는 이 스크립트가 만들기 전까지 손으로 만들지 않는다.

## 8. Tools/check_mcp_config.py [--check|--write|--include-home] — 정본 Tools/mcp_config_source.json(unreal-mcp URL·포트, blender 런처, serena 인자)에서 서버 `Config/DefaultEditorPerProjectUserSettings.ini` 포트와 클라이언트 5벌(.mcp.json·.codex/config.toml·.cursor/mcp.json·.gemini/settings.json(context 키 보존)·.agents/mcp_config.json; .vscode/mcp.json은 D-30으로 제외, 차이는 보고만)의 해당 키만 갱신하고 `~/.gemini/config/mcp_config.json`(Antigravity `~/.gemini/antigravity/mcp_config.json`은 이 파일의 심볼릭 링크)과의 차이를 보고만(홈 쓰기는 --include-home 명시 시만). 홈 blender 항목이 `uvx blender-mcp`·`mcp-for-blender`·pip 설치본이면 텔레메트리 보호가 없으므로 경고(D-36).

## 9. Git 훅 — `git config core.hooksPath Tools/githooks`를 ue_editor.py ensure가 자동 설정, pre-commit이 `check_agent_ops.py --repo --tools`를 호출해 종료 코드 2면 중단·1은 경고. 커밋이 드물므로 주 검사는 deferred 3 세션 게이트.

## 10. SessionStart 훅 + 세션 저널(단일 사양, D-35 조건부) — `Tools/check_agent_ops.py --journal --agent <claude|codex|gemini|antigravity|cursor> --event <post_tool|post_tool_failure|session_end|stop|stop_failure>`가 stdin JSON을 정규화해 `Saved/AgentOps/journal/<날짜>-<agent>-<세션8>.jsonl`(git 제외)에 추가 전용으로 쓴다. 필드는 허용 목록(도구 이름·저장소 상대 경로·종료 코드·출력 바이트·sha256·시각)만, 자유 텍스트 3필드(명령 200자·마지막 발화 2KB·live 파일 2KB)는 마스킹 정규식(`(api[_-]?key|token|secret|password|bearer)\s*[=:]\s*\S+` → `***`)을 거친다. SessionStart 주입은 `--start` 출력(≤40줄) 끝에 `lock: <status 요약>` / `인수 초안: <경로|없음>` 2줄만 덧붙인다. Claude 훅 1벌부터(PostToolUse 매처 `Edit|Write|MultiEdit|NotebookEdit|mcp__.*` async, PostToolUseFailure, Stop, StopFailure, SessionStart, SessionEnd timeout 1); Codex Desktop·Antigravity Windows·Gemini·Cursor는 훅 발화 실측 뒤. 착수 시 연속 편집 12회 중 발화 수 1회 실측(Docs/Validation). 정적 규칙은 주입하지 않고 SessionEnd로 기록을 강제하지 않는다. 훅 스크립트는 모든 경로에서 종료 코드 0.

## 11. Tools/lib/resource_lock.py + Tools/lock_resource.py(D-32 조건부: 동시 세션 운용 결정 시) — `Saved/Locks/<editor|blender>.lock/` mkdir 원자 획득, owner.txt(agent·세션ID8·시각·mode), lock.log 추가 전용. stale = owner.txt mtime 경과 > TTL(수치는 스크립트 상수에만). 소유자 자신이 다시 부르면 mtime touch로 무비용 heartbeat. 자원 editor는 `open|closed` 모드를 갖고 Build.bat·UnrealEditor-Cmd 헤드리스 테스트는 closed 모드 획득(에디터 종료 확인) 뒤에만 실행한다(라이브 코딩 뮤텍스·DLL 잠금 때문에 에디터 실행 중 빌드 불가, `ue_editor.py` cmd_build도 종료 코드 2로 거부). 진입부 검사 5곳: `ue_editor.py ensure/restart/stop/build`, `uemcp.py`, `run_in_editor.py`, `BlenderMCP/call_tool.py`. 잠금 루트는 `git rev-parse --path-format=absolute --git-common-dir` 상위. CLI: `python Tools/lock_resource.py acquire <editor|blender> --agent <agent> [--mode open|closed]` / `release` / `status`. NOW에 자원 줄을 두지 않고 AGENTS.md는 바꾸지 않는다(정본은 `status` 출력). 죽은 세션의 잠금은 TTL 경과 또는 사용자 확인 뒤 회수. 벤더 MCP 클라이언트로 unreal-mcp를 직접 호출하는 경로는 잠금이 못 막으므로 'uemcp.py 경유' 규칙과 병행. 표준 라이브러리만(os.mkdir·os.stat·os.utime·json·subprocess), 라이브러리 90~110줄 + CLI 30줄 + 진입부 패치 5×5줄.

## 12. 낡음 검출(deferred 3 --docs 확장) — 구현 시 Docs/README.md 끝에 `마지막 점검 커밋: <SHA>` 줄을 처음 도입하고 `git diff --name-only <SHA>..HEAD`로 Source/·Tools/ 변경 파일과 Lessons 범위·증거 경로, Tools/README 표, Docs/README 경로, 손 유지 표의 `확인:` 줄 경과일을 대조해 '재검토 필요' 목록을 출력하고 통과 시 SHA 줄을 갱신(그전까지 SHA 줄 없음).

## 13. Tools/draft_session_handoff.py(D-34 조건부: 죽은 세션 1회 관측 + 앱 스레드 마지막 메시지 복원 실패 뒤) — 모델 호출 0으로 인수 초안 `Saved/AgentOps/handoff/<agent>-<작업ID>.md` 생성. Codex: `~/.codex/session_index.jsonl`(id·thread_name·updated_at)로 후보를 줄인 뒤 rollout JSONL(`~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl`)의 `session_meta.cwd`가 저장소인지 확인, 죽은 세션 판정은 `task_started`에 짝(`task_complete`·`turn_aborted`·`error`)이 없고 `source≠subagent`이며 마지막 레코드 후 무기록 경과 > 상수이고 Worklog 마지막 `시작` 항목 담당이 codex일 때. 복원 항목: `item_completed` 5종의 파일 경로 순서(FileChange는 키만, 내용 제외), CommandExecution의 command·cwd·exit_code·status(1차 소스; code-mode `exec` JS 정규식은 폴백), 마지막 AgentMessage 2KB, reasoning `summary[].text` 제목 N개(각 ≤120자; 상세 추론과 하위 에이전트 메시지는 암호화라 불가). Claude: `~/.claude/projects/<경로>/<session>.jsonl`의 tool_use 파일 경로·마지막 assistant 텍스트. 자유 텍스트는 10번과 같은 마스킹. 할당량 소진 rollout의 실제 레코드 형태는 첫 발생 때 실측해 파서를 갱신한다. 인수자는 초안을 사실로 쓰지 않고 git diff·빌드·테스트로 재검증한다. 초안 보존 기간은 D-37 이후 결정. 표준 라이브러리만, 170~220줄.

## 14. 경미 항목 — `Tools/uemcp.py`가 종료 시 MCP 세션 DELETE 요청을 보내지 않아 엔진 서버에 세션이 누적된다(SESSION_FILE 상수는 정의만 있고 쓰이지 않으므로 세션 덮어쓰기 문제는 없음). 종료 시 DELETE 1줄 추가. `Tools/append_worklog_entry.py`(msvcrt 잠금 아래 단일 append + 형식 검증, 60~80줄)는 Worklog 항목 유실이 1회 관측된 뒤에만.

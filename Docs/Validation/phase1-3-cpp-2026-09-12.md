# Phase 1~3 C++ 이식 검증 (2026-09-12)

## 빌드·자동화 테스트
- 모듈 `TDWorldGen`(신설), `TDGameEditor/WorldGen`, `TDGame/World` 추가 후 Build.bat 성공.
- `Automation RunTests TDGame` 37개 전부 Success(`Saved/Logs/AllTests.log`): 전투 회귀 28, `TDGame.WorldGen.Dungeon.*` 4(5흐름×3크기×시드 1~100 = 1,500건 100%, KeyLock 시드 1~200 순서 위반 0, 해시 동일, 문 제거 시 실패), `TDGame.WorldGen.World.*` 4(평지 시드 1~50 통과율 100%, 도로 도달, 해시 동일, 슬롯 간격 오류 검출), `TDGame.WorldState.ActorRecordRoundTrip` 1.
- 커맨드릿 `-run=TDWorldGen -Flow=KeyLock -Size=Medium -Seeds=1-5` → 5/5 통과, 상위 3 추천(`commandlet-dungeon-sweep.md`).

## 에디터 통합
- 정의 에셋 생성(`editor_make_definitions.py`): DA_TDWorld_Main, DA_TDRegion_Forest, DA_TDBiome_Forest, DA_TDDungeonAtlas_Main(3슬롯), DA_TDPoi_4종, DA_TDTheme_Crypt(모듈 12), DA_TDFlow_5종. `Config/DefaultGame.ini`에 `[/Script/TDWorldGen.TDWorldGenSettings]` 기본 아틀라스 등록.
- 룸 모듈 레벨 12개(`Content/Dungeon/Rooms/Crypt/LI_TDRoom_Crypt_*`, 바닥·벽·문 마커·내비 볼륨) 생성, 테마에 연결.
- C++ 베이커로 3슬롯 베이크·저장(`cpp_dungeon_slot0.jpg`, `cpp_dungeon_slot0_boss.jpg`), 슬롯 정의에 Entry/Exit·Bounds 기록.
- C++ 월드 생성기(랜드스케이프 샘플러) seed 7: POI 15, 입구 4, 도로 25, 검증 100/100(`cpp-world-layout-seed7.md`; POI 쿼터 상향 전에는 POI 5개·무콘텐츠 구간으로 70점).
- 입구·출구 액터 3쌍 배치(`field_entrance.jpg`).

## PIE 심리스 이동 왕복
- `TDTravelToDungeon MainCrypt`: Idle→Preloading→ReadyToTravel(0.48s)→Traveling→Settling→Idle, 폰 (−10600, 2600) → (305000, 303400).
- `TDTravelToField`: 10.5초 후 (29621, −26810, 2237) 귀환(필드 셀 재로드 + 동적 내비메시 생성 시간).
- 에디터 재시작 직후의 첫 시도는 내비메시가 아직 없어 20초마다 경고만 내고 대기(설계 R-62: 미완료 이동 금지) — 내비메시 정적 생성·월드 파티션 청크(P1-02)로 개선 예정.

## 남은 것
- P1-02 내비메시 월드 파티션 설정(정적 생성·청크), P1-05 걸어서 트리거 왕복, P1-06/07 스트리밍 후 상자 상태 유지 PIE 검증, P1-08 자동화 테스트.
- P2-03 규격 검사 커맨드, P2-08 에디터 버튼 UI, P2-10~12, P3-06 잠금·부분 재생성, P3-07 PCG 바이옴, P3-09 버티컬 슬라이스 측정, P3-10 월드 파티션 빌더 파생.
